{
  "name": "video-stream-detector",
  "version": "1.0.0",
  "description": "Auto-detect m3u8/HLS video stream URLs using Playwright headless browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-browser": "npx playwright install chromium",
    "setup": "npm install && npm run install-browser"
  },
  "keywords": [
    "m3u8",
    "hls",
    "video",
    "stream",
    "detector",
    "playwright"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
